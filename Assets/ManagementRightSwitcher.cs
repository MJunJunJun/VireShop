using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ManagementRightSwitcher : MonoBehaviour
{
    public bool switcherModeOn;
    // Start is called before the first frame update
    void Start()
    {
        switcherModeOn=false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
